itemloop = True
while itemloop:
    items = input("Enter num item:")
    if items.isnumeric():
        itemloop = False
    else:
        print("item error:")

centloop = True
while centloop:
    cents = input("Enter cents per item:")
    if cents.isnumeric():
        dollar = int(cents) / 100
        centloop = False
    else:
        print("cent error")

if int(items) < 10:
    tcost = int(items) * dollar
if int(items) >= 10 and int(items) < 20:
    tcost = int(items) * (dollar * 0.9)
if int(items) >= 20:
    tcost = int(items) * (dollar * 0.8)

print(tcost)
